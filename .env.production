# Display mode for the deployed app.
# Default = REAL numbers (absolute revenue, customer counts, and LTV are shown).
# Set NEXT_PUBLIC_SAFE_MODE=1 ONLY for a public/shareable deploy to hide absolutes.
# (Local `npm run dev` always shows full numbers regardless.)
NEXT_PUBLIC_SAFE_MODE=0
